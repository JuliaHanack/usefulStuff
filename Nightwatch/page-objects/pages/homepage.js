// NOTE: testdata & config not included in this project
const TestData = require('../../testdata.js');

module.exports = {
    url() {
        return `${TestData.url}/`;
    },
    commands: [],
    elements: {}
};
